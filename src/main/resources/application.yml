spring:
  application:
    name: Troublog
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:local}

  ai:
    openai:
      api-key: ${OPEN_AI_API_KEY}
      chat:
        options:
          model: gpt-4.1-mini
  mvc:
    log-resolved-exception: false