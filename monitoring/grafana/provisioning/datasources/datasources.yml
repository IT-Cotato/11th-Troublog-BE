apiVersion: 1

datasources:
  # Prometheus 데이터소스
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  # Loki 데이터소스
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    editable: true